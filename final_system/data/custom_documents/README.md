# Custom Documents

Place custom legal documents in this folder.

Supported formats:

- `.txt`
- `.pdf`
- `.docx`

The final RAG runner will read these files, split them into chunks, retrieve relevant chunks for each benchmark question, and generate answers using the selected LLM mode.